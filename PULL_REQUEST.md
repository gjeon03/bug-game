# 품질 리부트 — 한국어 전면화, 오프라인 베이크 아트 파이프라인, 배포 검증

`pre-quality-reboot`(368d1b0) → `main`(8daeab6) · 48 커밋 · 143 파일 · +8488 / −1833

배포되어 동작 중: **https://gjeon03.github.io/bug-game/**

---

## 왜 이 작업을 했나

보고된 결함 12건을 스크린샷으로 전부 재현하는 것에서 시작했다. 접시는 동심원, 배수구는 십자 원,
화면을 지배하는 청흑색 대형 사각형, UI 100% 영어.

근본 원인 하나는 코드가 아니라 문서에 있었다. `ART_BIBLE.md`가 **"캐비닛과 가전은 프롭이 아니라
벽"** 이라고 명시하고 있었다. 대형 사각형 결함은 아트가 방향에서 벗어난 것이 아니라, 틀린 방향을
충실히 수행한 결과였다. 그 규칙은 폐기 사유와 함께 명시적으로 철회했다.

---

## 검증 — 배포본을 '로드'가 아니라 '플레이'해서 확인

`tests/e2e/deployed.spec.ts`가 공개 URL을 상대로 실행된다. CI에서는 제외(네트워크 의존 금지),
`pnpm test:deployed`로 의도적으로 돌린다.

| 항목                           | 결과                                                           |
| ------------------------------ | -------------------------------------------------------------- |
| 플레이 가능까지                | 1881 ms                                                        |
| title / lang                   | `걸레받이 제국` / `ko`                                         |
| NanumSquareNeo 실제 사용       | 123.6 px 대 폴백 114.9 px                                      |
| HUD의 플레이어용 영어          | **0개**                                                        |
| 전달 루프 완주                 | 배달 15회, 연결된 길 2개, 군체 6 → 10                          |
| 오디오                         | 작동, 피크 보이스 2 — 무음 아님                                |
| 재시작 5회                     | 311/309/309/272/312 ms, 전부 `routes=0 hazards=0 deliveries=0` |
| 프레임 예산                    | cpuP50 1.7 · cpuP99 2.2 · 최악 3.8 ms (예산 8 ms)              |
| 오리진 밖 요청                 | **0**                                                          |
| 페이지 · 콘솔 오류 · 실패 요청 | **0 / 0 / 0**                                                  |

CI 게이트 17개 전부 통과(`8bd4dc5`). 베이스라인에서 실패하던 2건(`fullrun 09`, `perf 14`)이
모두 통과로 바뀌었고 **예산 값은 손대지 않았다.**

---

## 주요 변경

### 1. 한국어 전면화 (349키)

- `ko-KR`이 기본이자 유일한 출시 로케일. `en`은 개발용 대조군.
- 용어는 맥락에서 결정: `evidence → 흔적`(증거는 법정 어감), `extermination → 박멸`,
  `cleaning → 걸레질`, `nymph → 약충`(바퀴는 유충 단계가 없다).
- 경보는 형용사가 아니라 어조로 상승: 조용함 → 낌새 → 바퀴 의심 → 방역 호출 → 박멸.
- **조사를 값의 '소리'로 계산.** 조사는 앞말의 소리로 결정되고 숫자는 '읽는 소리'다. 24는
  이십사라서 `가`, 18은 십팔이라서 `이`. 카탈로그가 `{amount}이`처럼 박아두어 도달 가능한 값의
  절반에서 틀렸다. `{amount}{amount?이/가}`를 `t()`가 해석한다.
- `word-break: keep-all` — 없으면 브라우저가 '박멸'을 '박'/'멸'로 쪼갠다.
- CSS `content`에 박혀 있던 영어(`⚠ CRITICAL`)도 카탈로그로 이동.

### 2. 오프라인 베이크 아트 파이프라인

이 기기에는 Blender도 ffmpeg도 없다. 대신 Playwright Chromium이 SwiftShader로 WebGL2를
지원한다 — 느리지만 **결정적**이라 스크린샷이 비교 가능한 증거로 남는다.

`tools/bake/`가 실측 밀리미터로 모델링한 3D 프롭을 **단일 카메라·조명 리그**로 16배 슈퍼샘플링해
스프라이트 시트 하나로 굽는다. three.js는 devDependency이며 **런타임 번들에 없다**(번들 내
`three` 매치 1건은 `en` 카탈로그의 영단어 "three").

- 척도: 정찰병 26유닛 = 35 mm. 접시 200 mm는 149유닛 — 정찰병 5.7마리 폭.
- 카메라를 수직에서 26° 기울여 물체가 앞면과 높이를 갖는다. **시뮬레이션은 2D 그대로.**
- 44프레임. 모든 `PropKind`가 '구움' 또는 '의도적 절차적'으로 분류되며, 분류하지 않은 종류를
  추가하면 테스트가 깨진다.

### 3. 바퀴벌레 (결함 5)

앞가슴등판·딱지날개·납작한 배·길이가 다른 세 쌍의 다리·몸보다 긴 더듬이·꼬리털. 화물은 떠 있지
않고 큰턱에 물려 있다.

진드기처럼 나온 원인은 취향이 아니라 부호 버그였다. three.js에서 +X를 Y축으로 θ 돌리면
`(cos θ, 0, −sin θ)`이므로 **양각은 전방을 향한다.** 46/88/132도는 세 쌍 전부를 앞으로 보냈고
뒷다리가 배 밑에 가려졌다. 앞 52 / 중간 6 / 뒤 −46으로 교정.

### 4. 없던 주광원 + `surfaceOnly`

광원 6개 중 어느 것도 조리대와 아일랜드에 닿지 않았다. **조명이 없는 물체는 아무리 잘 모델링해도
실루엣일 뿐이다.**

노출이 같은 필드를 샘플링하므로 이건 게임플레이 수치이기도 했고, 두 번 물렸다. 두 번째는 신중한
플레이가 `water=0`으로 굶어 죽었다 — 공정성 실패다. 대조 실험으로 확정했다(광원 제거 2/2 통과,
포함 1/2).

원인은 밝기가 아니라 **차폐 부재**였다. 하부등은 상판을 비추고 그 앞 바닥은 캐비닛 그늘에 든다.
이 모델에는 차폐가 없어 콜로니의 물길을 밝히고 있었다. `LightSource.surfaceOnly`가 해당 광원을
노출 필드에서 빼고 렌더러는 그대로 그린다. 아일랜드 펜던트는 폐기 —
**통행로 위의 조명은 아트 옷을 입은 난이도 변경이다.**

### 5. 성능 — 계측이 다섯 번의 추측을 한 번에 끝냈다

| 측정                    | 최악 프레임 | 지배적 단계                 |
| ----------------------- | ----------- | --------------------------- |
| 계측 전                 | 43.7 ms     | 불명 — 가설 5개 전부 빗나감 |
| 계측 후                 | 41.6 ms     | `overlays` **39.8**         |
| 오버레이 수정 후        | 35.0 ms     | `bodies` **33.7**           |
| 스프레이·발소리 수정 후 | **통과**    | —                           |

두 원인 모두 같은 버그: **불변인 `CanvasGradient`를 매 프레임 새로 만드는 것.** 비네트는 프레임당
2개, `drawSprays`는 구름당 22개.

**아트는 원인이 아니었다.** 프롭·바디·엣지 스트립을 다 합쳐 0.3 ms이고, 그게 정확히 앞선 다섯
가설이 겨냥했던 대상이다. 지속 비용(`active-play cpuP99`)은 별개로 32.3 → 2.0 ms.

### 6. CI 관측 가능성

배포가 실패했는데 진단이 불가능했다. CI가 남긴 것은 `exit code 1` 한 줄이고 상세는 인증이 필요한
아티팩트 안에만 있었다. 로그도 403, 잡 요약도 API에 안 나온다. **어노테이션은 나온다.**

이제 실패 시 `::error::`로 실패한 스펙·파일·줄·에러, 안내 플레이의 결정 집계, 최악 프레임의 단계별
분해를 방출한다. 자격증명 없이도 배포 실패를 진단할 수 있다.

그 채널이 만든 차이: `water=0` 한 필드가 "내가 너무 위험하게 만들었다"에서 "목말라 죽었다"로
방향을 돌렸다 — **정반대의 수정**이다.

---

## 아직 안 된 것 (숨기지 않음)

`ASSET_MANIFEST.md`에 완료 차단 항목으로 기록되어 있다.

- **조리대·가전 상판이 여전히 평면 채우기.** 엣지(손잡이·줄눈·토킥)는 들어갔지만 상판은 아니다.
- **구역 판독은 6개 중 4개.** 싱크런·식기·레인지·팬트리는 라벨 없이 읽힌다. 냉장고는 부분적,
  아일랜드·문간은 프롭 배치 이후 재평가하지 않았다.
- **승리·패배 시나리오는 배포본이 아니라 로컬 증거만** 있다.
- **`fullrun 09`는 간헐적이다.** 이번 실행은 통과했지만 한 번의 통과는 안정성의 증거가 아니다 —
  이 세션에서 그 주장을 이미 한 번 잘못했다. 게임 자체가 공정하다는 것은 결정론적으로 증명했다
  (루틴을 한 번도 안 쫓는 군체가 4분을 버틴다). 남은 것은 안내 플레이어의 타이밍 취약성이다.
- 오디오는 절차적 합성이며 *의도된 최종*으로 분류. 샘플 파일 없음은 누락이 아니라 결정이다
  (바이트 0, 라이선스 불필요, 서버리스 유지). 배포본에서 실제로 울리는 것을 측정했다.

---

## 이 PR을 어떻게 볼 것인가

`main`에 이미 반영된 상태다. Pages 워크플로가 `main` 푸시에서만 배포되고, 브리프가 "배포된 URL을
실제로 플레이해서 검증"할 것을 요구했기 때문에 거기서 작업했다.

이 PR은 **리뷰용**이다. `pre-quality-reboot`는 리부트 직전 상태(368d1b0)를 가리키는 마커
브랜치이며 병합 대상이 아니다.

---

## 방법론 메모

이 세션에서 해결한 문제는 전부 **계측하거나 대조군을 세워서** 풀렸다 — CI 어노테이션 채널,
조명의 lights-out/lights-in 실험, "루틴이 필수인가"를 9초 만에 답한 결정론적 테스트.
**추측해서 접근한 문제는 다섯 번, 여섯 번씩 걸렸다.**

총합만 있고 분해가 없는 프레임 예산은 비용이 0인 곳을 고치게 만든다. 그것이 이 리부트에서 가장
비쌌던 교훈이다.
